"use strict";
// Functions
function add(n1, n2) {
    return n1 + n2;
}
function printResult(num) {
    console.log(num);
    // return;//when undefined is used
}
let combineValues;
combineValues = add;
combineValues(4, 3);
